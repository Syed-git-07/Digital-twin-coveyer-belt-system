# Local Docker defaults. Set your HTTPS domain and secure cookies on a VPS.
PORT=8090
SECURE_COOKIES=false
ALLOWED_ORIGINS=http://localhost:8090,http://127.0.0.1:8090
# Optional for localhost; set a long random token before exposing initial setup remotely.
SETUP_TOKEN=
CONVEYOR_KEEP_RUNS=50
